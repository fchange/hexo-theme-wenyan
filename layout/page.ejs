<section class="page-heading">
  <h1><%= page.title %></h1>
  <% if (page.description || config.subtitle) { %>
    <p><%= page.description || config.subtitle %></p>
  <% } %>
</section>

<section class="page-shell">
  <article class="post-content page-content">
    <%- page.content %>
  </article>
</section>
